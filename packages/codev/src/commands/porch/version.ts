/**
 * Porch protocol orchestrator version.
 * This is intentionally a standalone constant (not derived from package.json)
 * because it tracks the porch protocol version, not the package release version.
 */
export const PORCH_VERSION = '1.0.0';
