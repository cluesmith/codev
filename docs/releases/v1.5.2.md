# v1.5.2 Florence

Released: 2025-12-28

## Summary

Florence introduces **secure remote access** to Agent Farm via SSH tunneling. Access your dashboard from any device with a single command.

## New Features

- **0062 - Secure Remote Access**: One-command remote access via `afx start --remote user@host`
  - SSH tunnel handles authentication and encryption
  - Reverse proxy routes terminal traffic through single port
  - Works with cloud VMs, local robots, and any SSH-accessible machine

## Improvements

- **`afx stop` orphan cleanup**: Now detects and kills orphaned agent-farm processes
- **`codev update` legacy cleanup**: Removes deprecated `codev/bin/` bash scripts
- **WebSocket proxy error handling**: Graceful handling of proxy errors
- **Port conflict detection**: Clear error messages when dashboard port is in use

## Deprecations

- **`--allow-insecure-remote`**: Deprecated in favor of `afx start --remote`. Shows warning when used.

## Breaking Changes

None

## Migration Notes

None required. Existing `afx start` usage unchanged.

## Usage

```bash
# Start Agent Farm on a remote machine
afx start --remote user@192.168.1.100

# With explicit project path
afx start --remote user@host:/path/to/project

# Skip browser opening (for scripts)
afx start --no-browser
```

## Known Limitations

- File annotation tabs don't work through the SSH tunnel (terminals only)
- Requires SSH server on the remote machine

## Contributors

- Human + AI collaboration via Codev
